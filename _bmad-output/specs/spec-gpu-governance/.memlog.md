---
topic: University AI Compute Management Platform — deterministic academic GPU governance layer (SPEC.md + ARCHITECTURE-SPINE.md, merged log)
updated: 2026-08-31T18:40
---

- (decision) sources reviewed: design-artifacts/A-Product-Brief (01-product-brief.md, rich, WDS methodology), Spec1 PDF (already Contract/Why/Capabilities/Constraints/Non-Goals/Success Signal/Assumptions-shaped — near 1:1 with kernel), course brief PDF (assignment context). B-Trigger-Map/C-UX-Scenarios/D-Design-System are unstarted phase-hub templates, not source content.
- (decision) input is rich and pre-sorted (Spec1 already carries kernel shape) — extracting directly, no elicitation needed.
- (capability) CAP-1 intent: system associates each request (access/usage, job-execution, reservation) with the requester's academic context (course, group, privilege level) before evaluating it. success: every evaluated request's decision record shows a resolved academic-context value at the point of evaluation; none are evaluated with an unresolved context.
- (capability) CAP-2 intent: system denies an access/usage, job-execution, or reservation request when the requester's academic context is not authorized for the requested model, service, workload type, or resource tier. success: an unauthorized request is denied Not Authorized regardless of resource availability; an authorized request is never denied for authorization reasons.
- (capability) CAP-3 intent: system denies a job-execution request that violates the time-window and/or authorized-user restriction defined for its workload type. success: a request outside its workload type's allowed window or user set is denied; combination logic across simultaneously-applicable restrictions is an open question, not asserted.
- (capability) CAP-4 intent: system denies any request when no resource currently satisfies it at evaluation time, with no holding, queuing, or rescheduling. success: a resource-unavailable request receives a terminal decision within that same evaluation, never held or auto-retried.
- (capability) CAP-5 intent: system creates a reservation for GPU/VRAM resources tied to an academic activity over a defined time window. success: once created, the resource shows reserved for that activity during its window, and conflicting general-use requests submitted in that window are denied (CAP-6).
- (capability) CAP-6 intent: system detects a new request that conflicts with an existing reservation or operational restriction and rejects it. success: a colliding request is denied, and the response names the specific conflicting reservation/restriction.
- (capability) CAP-7 intent: system reports, for any denied request, the specific attributable cause (authorization rule, quota, resource unavailability, or reservation/restriction conflict). success: the requester can identify the exact cause directly from the response — never a generic rejection.
- (capability) CAP-8 intent: system reports the current status of any tracked machine as available, reserved, busy, idle, or failing. success: a lab operator can retrieve a tracked machine's current status on demand.
- (capability) CAP-9 intent: system records token-level usage (prompt/generation tokens) and job-level usage per user/course for every accepted request. success: every accepted request produces a queryable usage record — requester, resource, time, and token- or job-level consumption.
- (capability) CAP-10 intent: system produces a usage summary, per user or academic context, of resource consumption over a specified period. success: a usage summary for a given user/context and time period can be retrieved, reflecting the usage records generated during it.
- (capability) CAP-11 intent: system restricts high-VRAM, exclusive, or distributed-training requests to users/contexts explicitly marked as authorized for them. success: such a request is denied for authorization reasons when the requester isn't marked authorized, and is not denied for authorization reasons when marked authorized, independent of separate unavailability/conflict denial.
- (constraint) heterogeneous GPU fleet: mostly 24GB VRAM machines, one dual-48GB machine — acceptance decisions must account for this variance.
- (constraint) permission and quota rules are tied to academic context (course/group/privilege level), not identity alone.
- (constraint) not all models/services are available to all subjects or courses.
- (constraint) workload types (e.g. training jobs) must be restrictable by time window and/or authorized-user set; how the two combine per workload type is unresolved (open question).
- (constraint) reservations must guarantee resource availability for their scheduled academic window — implemented as full precedence over general use, a risk-ranked assumption not an explicit source requirement (see assumptions).
- (constraint) usage must be tracked and reportable in governance terms (token consumption, infrastructure utilization) — never for billing or chargeback.
- (constraint) machine-level operational status must be visible at minimum to lab operators; broader role-based visibility scope is unresolved (open question).
- (constraint) execution context is a Kubernetes-managed GPU fleet exposing GPUs via device plugins, with Kubeflow Training Operator-style multi-node distributed training — locked context, not a design choice of this spec.
- (constraint) no latency/throughput/SLA figures exist in source beyond sub-second deterministic evaluation; none may be invented.
- (constraint) real-time/near-real-time delivery (e.g. WebSocket) is required for live fleet status; no offline mode, offline-sync, or service-worker caching architecture — state must always reflect current reality.
- (constraint) desktop-first responsive web app; mobile is a lightweight read-only companion view, not a full-parity surface.
- (non-goal) final internal module decomposition of the platform.
- (non-goal) exact database schema.
- (non-goal) exact user interface design, frontend styling, or university branding guidelines.
- (non-goal) final deployment architecture.
- (non-goal) exact technology stack beyond the established Kubernetes/Kubeflow context.
- (non-goal) billing, invoicing, or financial chargeback mechanisms of any kind.
- (non-goal) request queuing or waitlisting when no resource is available, for this iteration (whether to add it later is an open question, not assumed either way).
- (non-goal) native mobile apps, desktop binaries, PWA, or offline functionality.
- (decision) success signal set to the primary metric (zero class interruptions per semester due to GPU conflicts, overriding all other metrics) combined with the Contract's determinism guarantee (same request + unchanged state => same outcome) — both concrete/demonstrable, both from source.
- (assumption) user identity, course/group membership, and privilege level are already available via an existing institutional directory; this platform is not responsible for creating that identity data. (SAFE)
- (assumption) academic context is one attribute set per request (course + group + privilege level), not a more complex structure like multiple concurrent enrollments with different permissions. (RISKY)
- (assumption) usage tracking for inference is token-based (prompt/generation tokens). (SAFE)
- (assumption) reservations take full precedence over general usage during their window, with no partial sharing or negotiation. (RISKY)
- (assumption) the platform tracks usage for governance/reporting only — never billing or financial chargeback. (SAFE)
- (assumption) an already-running job is not preempted by a later-arriving, higher-priority reservation; preemption would be an additional capability if required. (RISKY)
- (assumption) Kubernetes with GPU device plugins and Kubeflow-style distributed training is established operational context and does not need to be designed by this spec. (SAFE)
- (question) should the platform support queuing/waitlisting a request when no resource is available? Explicitly unresolved by source, out of scope this iteration either way.
- (question) for a given workload type, do time-window and authorized-user restrictions both have to hold (AND), does either suffice (OR), or are they applied independently per workload type?
- (question) should machine operational status be visible to roles beyond lab operators (e.g. students, teachers), and if so at what detail level?
- (constraint) heterogeneous GPU fleet: mostly 24GB VRAM machines, one dual-48GB machine — acceptance decisions must account for this variance.
- (constraint) permission and quota rules are tied to academic context (course/group/privilege level), not identity alone.
- (constraint) not all models/services are available to all subjects or courses.
- (constraint) workload types (e.g. training jobs) must be restrictable by time window and/or authorized-user set; how the two combine per workload type is unresolved (open question).
- (constraint) reservations must guarantee resource availability for their scheduled academic window — implemented as full precedence over general use, a risk-ranked assumption not an explicit source requirement (see assumptions).
- (constraint) usage must be tracked and reportable in governance terms (token consumption, infrastructure utilization) — never for billing or chargeback.
- (constraint) machine-level operational status must be visible at minimum to lab operators; broader role-based visibility scope is unresolved (open question).
- (constraint) execution context is a Kubernetes-managed GPU fleet exposing GPUs via device plugins, with Kubeflow Training Operator-style multi-node distributed training — locked context, not a design choice of this spec.
- (constraint) no latency/throughput/SLA figures exist in source beyond sub-second deterministic evaluation; none may be invented.
- (constraint) real-time/near-real-time delivery (e.g. WebSocket) is required for live fleet status; no offline mode, offline-sync, or service-worker caching architecture — state must always reflect current reality.
- (constraint) desktop-first responsive web app; mobile is a lightweight read-only companion view, not a full-parity surface.
- (non-goal) final internal module decomposition of the platform.
- (non-goal) exact database schema.
- (non-goal) exact user interface design, frontend styling, or university branding guidelines.
- (non-goal) final deployment architecture.
- (non-goal) exact technology stack beyond the established Kubernetes/Kubeflow context.
- (non-goal) billing, invoicing, or financial chargeback mechanisms of any kind.
- (non-goal) request queuing or waitlisting when no resource is available, for this iteration (whether to add it later is an open question, not assumed either way).
- (non-goal) native mobile apps, desktop binaries, PWA, or offline functionality.
- (decision) success signal set to the primary metric (zero class interruptions per semester due to GPU conflicts, overriding all other metrics) combined with the Contract's determinism guarantee (same request + unchanged state => same outcome) — both concrete/demonstrable, both from source.
- (assumption) user identity, course/group membership, and privilege level are already available via an existing institutional directory; this platform is not responsible for creating that identity data. (SAFE)
- (assumption) academic context is one attribute set per request (course + group + privilege level), not a more complex structure like multiple concurrent enrollments with different permissions. (RISKY)
- (assumption) usage tracking for inference is token-based (prompt/generation tokens). (SAFE)
- (assumption) reservations take full precedence over general usage during their window, with no partial sharing or negotiation. (RISKY)
- (assumption) the platform tracks usage for governance/reporting only — never billing or financial chargeback. (SAFE)
- (assumption) an already-running job is not preempted by a later-arriving, higher-priority reservation; preemption would be an additional capability if required. (RISKY)
- (assumption) Kubernetes with GPU device plugins and Kubeflow-style distributed training is established operational context and does not need to be designed by this spec. (SAFE)
- (question) should the platform support queuing/waitlisting a request when no resource is available? Explicitly unresolved by source, out of scope this iteration either way.
- (question) for a given workload type, do time-window and authorized-user restrictions both have to hold (AND), does either suffice (OR), or are they applied independently per workload type?
- (question) should machine operational status be visible to roles beyond lab operators (e.g. students, teachers), and if so at what detail level?
- (decision) spawning companion stakeholders.md — per-entity matrix (5 stakeholder groups: need, frustration, today's coping, primary/secondary) from product-brief ICP section; too multi-item for kernel prose.
- (decision) spawning companion tone-of-voice.md — editorial voice rules (4 tone attributes, do/don't, worked examples) from product-brief Tone of Voice section; load-bearing for microcopy/denial-message wording downstream.
- (decision) competitive landscape (Slurm/PBS/raw K8s/buy-more-GPUs comparison) and behavioral-shift narrative from product-brief are wrapper-only color supporting Why/Success signal — not separately preserved verbatim; core reasoning (paradigm gap, not feature gap) folded into Why.
- (note) sources fully absorbed, no companion needed, listed in frontmatter sources: design-artifacts/A-Product-Brief/01-product-brief.md, ../Spec1-Juan_Camilo_Molina_Mussen-A00399775.pdf, ../project - university_ai_compute_management_platform_brief.pdf (course assignment context).
- (event) self-validate pass 2 (preservation): found gap — Spec1 Contract's Accepted-outcome clause (system communicates allocated resource + conditions of use: time window, expiry) had no capability. Added CAP-12.
- (capability) CAP-12 intent: system communicates, on acceptance, which resource was allocated and its conditions of use (time window, expiry). success: an Accepted response names the specific allocated resource and its usage conditions directly in the response.
- (event) self-validate pass 1 (coherence) verdict: PASS. 12 capabilities each with intent+success; intents are WHAT not HOW; 11 constraints all genuinely bend design; 8 non-goals; success signal concrete/testable (zero interruptions + determinism repeatability); CAP-1..12 stable/unique; prose lean.
- (event) self-validate pass 2 (preservation) verdict: PASS after CAP-12 fix. Wrapper-only content not separately preserved: competitive-landscape comparison narrative (Slurm/PBS/raw-K8s/buy-more-GPUs), business-model rationale prose, timeline/checkpoint milestones, WDS process-hub files (00-product-brief.md, B-Trigger-Map, C-UX-Scenarios, D-Design-System — all unstarted templates, no content to preserve).
- (event) spec finalized: SPEC.md + companions stakeholders.md, tone-of-voice.md. 12 capabilities, 11 constraints, 8 non-goals, 7 assumptions, 3 open questions.
- (decision) input: SPEC-gpu-governance (12 CAPs, 11 constraints, 8 non-goals, 7 assumptions, 3 open Qs) is the driving input, richest available; design-artifacts WDS phase hubs (B/C/D) largely unstarted templates. Altitude: initiative (whole system, no epics yet).
- (decision) greenfield confirmed: no package.json/requirements.txt/go.mod/pom.xml in repo; no project-context.md found; no custom bmad-architecture.toml overrides.
- (decision) user chose Coaching path (not Fast) and Spine + presentation deliverable (interactive deck to walk through architecture) at activation.
- (decision) AD: Design Paradigm = Hexagonal / Ports-and-Adapters with pure deterministic core. evaluate(request, state) -> outcome is a pure function, zero infra side effects, matches SPEC success signal (identical request+state => identical outcome). Adapters: K8s/Kubeflow state, academic-context/identity resolution, reservation store, token/usage recorder, WebSocket broadcast. [ADOPTED — user confirmed, no pushback].
- (version) Stack chosen by user: Backend Python + FastAPI + kubernetes python client. Frontend React/Next.js + Tailwind CSS. Storage PostgreSQL (reservations, policy config, academic context, token usage logs) + Redis (active fleet state cache + WebSocket pub/sub). Deployment: in-cluster, alongside GPU workloads, via K8s API + Kubeflow Training Operator CRDs. [ADOPTED — user decision]
- (decision) User said 'proceed with drafting' after paradigm+stack confirmed — moving from open elicitation to draft-and-review for remaining structural calls (module boundaries, state ownership specifics), flagging genuine open items rather than re-asking settled ground.
- (version) Web-verified current versions (Aug 2026): FastAPI ~0.141.x (pre-1.0, active); kubernetes python client 36.0.3 (PyPI); Next.js 16.3.3 (Active LTS); PostgreSQL 18.6 (stable major, PG19 still beta); Redis 8.10.1; Tailwind CSS 4.3.3.
- (question) Kubeflow project moved from Training Operator V1 (kubeflow.org/v1 TFJob/PyTorchJob CRDs, latest v1.8.0) to Kubeflow Trainer V2 (trainer.kubeflow.org, v2.2 Mar 2026, different CRD: TrainJob). SPEC constraint says 'Kubeflow Training Operator-style' (locked context, not this spec's choice) — but which CRD API group the K8s adapter targets is a real implementation fork not settled by spec wording. Flagging as Deferred/open item: verify actual university cluster's installed Kubeflow version before binding adapter to v1 TFJob/PyTorchJob vs v2 TrainJob CRDs.
- (decision) AD-1 Outcome is a closed four-way sum type (Accepted|Denied:NotAuthorized|Denied:ResourceUnavailable|Denied:ReservationConflict). Binds CAP-2,3,4,6,7,12. Prevents a 5th outcome / held-pending state creeping in.
- (decision) AD-2 Evaluation synchronous, single-pass, returns within request/response cycle. Binds CAP-4. Prevents background retry/requeue on Resource Unavailable.
- (decision) AD-3 Reservations are state entries read by the same evaluate() snapshot, not a parallel conflict-checking path. Binds CAP-5,6.
- (decision) AD-4 PostgreSQL is sole source of truth for allocation state; commits use row-level locking before Redis/WebSocket write; Redis is derived/disposable cache only. Binds CAP-5,6,9. Prevents split-brain / double-allocation race.
- (decision) AD-5 Academic context resolved once at request entry via AcademicContextPort, passed into evaluate() as input, never re-resolved mid-core. Binds CAP-1,2,11.
- (decision) AD-6 Every Outcome carries structured cause-specific data (typed reason for Denied incl rule/quota/resource/reservation id; allocated resource+conditions for Accepted) -- never a bare string. Binds CAP-7,12.
- (decision) AD-7 Usage-record write is in the same transaction as the allocation commit (AD-4), never fire-and-forget post-response. Binds CAP-9.
- (decision) AD-8 Postgres state-change commits publish to Redis pub/sub; WebSocket gateway is sole fan-out; clients never poll REST for live status. Binds CAP-8.
- (decision) AD-9 domain/ (core+ports) has zero imports of FastAPI/K8s client/Postgres driver/redis-py; adapters depend inward on ports; composition root wires concrete adapters at startup. Binds all -- protects determinism guarantee from infra creep.
- (decision) Structural seed: system-context diagram (Web App -> FastAPI service -> Postgres/Redis/K8s+Kubeflow/external directory), core-entity ERD (AcademicContext/Request/Outcome/Reservation/Machine/UsageRecord), source tree (domain/adapters/api/web). Deployment: in-cluster ServiceAccount scoped RBAC (not cluster-admin) -- exact role Deferred per SPEC non-goal.
- (event) First full draft of ARCHITECTURE-SPINE.md written: Design Paradigm, 9 ADs, Consistency Conventions, Stack (web-verified versions), Structural Seed (3 diagrams + tree + deployment note), Capability-to-Architecture Map (all 12 CAPs), Deferred (9 items incl Kubeflow v1/v2 CRD fork + all 3 SPEC open questions carried forward).
- (event) Reviewer gate ran: lint_spine.py clean, 3 parallel subagents (rubric-checklist walker, version-currency researcher, adversarial-divergence attacker). Findings: 2 critical (missing evaluation-precedence rule violating SPEC CAP-2's explicit 'NotAuthorized regardless of availability'; diagram contradicted AD-5/pure-core claim by drawing evaluate() calling all 5 ports directly), 1 critical from adversarial (AD-4 contradicted its own diagram -- K8s wired as ResourceStatePort implementer alongside Postgres-source-of-truth claim, no mutual exclusion), 1 critical (AD-8 publish had no named owner + reservation-expiry unaddressed), plus high/medium findings on: sync k8s client vs async FastAPI unaddressed, no async Postgres driver pinned, Next.js 16 breaking changes unmentioned, AD-9 didn't ban stdlib datetime/random, AcademicContext/Outcome field shapes unspecified, isolation/locking strategy too loose ('or equivalent'), hexagonal boundary-bypass rule inconsistent (CAP-10/WS), no WS message schema, infra-failure-vs-Outcome mapping silent, observability dimension silent, deployment/RBAC stated as prose not AD, redis-py version unpinned, 'Active LTS' mislabel for Next.js.
- (decision) Applied fixes at Finalize (all clear, no new user judgment needed -- resolved from SPEC text + sound architecture reasoning): reworked orchestration diagram (driving adapters gather inputs -> pure evaluate() -> driving adapters commit/notify, core no longer shown calling ports); added AD-10 evaluation precedence (auth-class checks before reservation-conflict before availability, per SPEC CAP-2 literal text); rewrote AD-4 so Postgres allocation ledger is sole ResourceStatePort/ReservationPort implementer, K8s adapter only health-syncs INTO it, explicit READ COMMITTED + SELECT FOR UPDATE, no optimistic retry (ties back to AD-2); rewrote AD-8 naming the single commit-and-publish helper as sole publisher + added reservation-expiry sweeper rule; tightened AD-9 to ban stdlib datetime/time/random/uuid in core; added fixed field-shape blocks to AD-5 (AcademicContext) and AD-6 (Outcome/details); added AD-11 boundary-bypass rule (non-authorization reads may skip core, fixes CAP-10 ambiguity); promoted deployment/RBAC prose to formal AD-12; added WS message envelope + threadpool-for-sync-k8s-client rows to Consistency Conventions; added asyncpg 0.31.0 + redis-py 8.1.0 + Node>=20.9 to Stack table; added infra-failure-vs-Outcome line to AD-1; added observability + RESOURCE_UNAVAILABLE granularity + redis-py compat-matrix to Deferred. Declined one low-severity reviewer hunch (PostgreSQL 18.6 patch-cadence plausibility guess) since the version was independently web-verified against postgresql.org's own release announcement, a stronger source than the reviewer's arithmetic guess.
- (event) Reconciliation subagent checked spine against SPEC.md, stakeholders.md, tone-of-voice.md, product-brief. SPEC.md and product-brief: fully accounted for. Gap 1 (tone-of-voice.md): spine never referenced companion, AD-6's structured details couldn't alone produce tone-compliant human copy, no owner for ID-to-label resolution -- fixed via added AD-13 (server-side singular message composition in api/http, resolves ids via postgres, applies tone-of-voice.md rules) + added stakeholders.md/tone-of-voice.md to sources frontmatter. Gap 2 (stakeholders.md): Lab Operators' 'workload distribution' visibility need has no home -- SPEC's CAP-8 is narrower (per-machine status only), a SPEC/companion scope mismatch not invented-away here; added to Deferred naming the mismatch, flagged to user rather than silently either adding or dropping scope.
- (event) Presentation deck built and published as Claude Artifact ('The Governance Gate') -- scrolling walkthrough: hero/four-outcome thesis, hexagonal paradigm diagram, 6-step request-lifecycle narrative, 6 highlighted load-bearing ADs (of 13), system-context + ERD diagrams, stack table, 12-capability coverage grid grouped by theme, 5 still-open items incl workload-distribution SPEC/stakeholder mismatch flagged for user decision.
- (event) spine finalized
- (direction by user) Targeted remediation pass, not a full re-derive: fix exactly the 2 items peer-review-remediation.md marked Open/Follow-up (CAP-4 atomicity split; Kubernetes/Kubeflow and token-metering Constraint-vs-Assumption duplication), nothing else unless a real defect surfaces.
- (direction by user) Atomicity fix, proposed direction: either add CAP-13 to re-atomize the dropped future-window disclosure, or add a Non-Goal disclaiming predictive availability windows — asked agent to push back if either has hidden cost.
- (decision) Rejected the Non-Goal option: the future-window disclosure is a load-bearing Spec-1 Contract claim (Denied: Resource Unavailable bullet), so declaring it a Non-Goal would invent a scope cut the source never asked for, violating Spec Law rule 7 (preservation) — it isn't out of scope, it was just badly atomized. CAP-13 is the only option that both fixes atomicity and preserves the source claim.
- (capability) CAP-13 intent: system states a known future availability window in a Resource Unavailable denial only when an existing reservation already establishes when the resource becomes free; never infers, estimates, or promises a window from any other source. success: a Resource Unavailable denial for a resource with an existing future reservation includes that reservation's start time as the stated window; a denial for a resource with no existing future reservation states no window.
- (direction by user) Implementation Independence fix, proposed direction: remove Kubernetes/Kubeflow entirely from Constraints, keep the fact only in Assumptions (already SAFE) plus ARCHITECTURE-SPINE.md Stack Decisions; same duplication pattern flagged for token-based metering (CAP-9 vs a SAFE Assumption) — asked agent to challenge if wrong.
- (decision) Pushed back on full removal: task-spec2's own SPEC.md template lists 'Kubernetes device plugin limitations' as a canonical Constraints example, and rubric criterion 5 (Domain Realism, 10%) explicitly grades for Kubernetes/Kubeflow domain grounding in the spec bundle — deleting it from Constraints over-corrects against the assignment's own guidance for no Implementation-Independence gain, since CAP-1..12 already carry zero Kubernetes vocabulary (verified by re-read). The real leak Workshop 2 flagged is the self-undermining 'locked context, not a design choice of this spec' disclaimer, not the mention itself.
- (constraint) Rewrote (did not remove) the Kubernetes/Kubeflow constraint: platform must integrate with the university's existing Kubernetes-managed GPU fleet (device-plugin exposure, Kubeflow Training Operator for distributed jobs) rather than select, replace, or bypass that orchestration layer — states plainly what it bends instead of disclaiming consequence. Removed the duplicate SAFE Assumption restating the same fact: a constraint that genuinely bends design (Spec Law rule 3) belongs in exactly one place.
- (decision) Token-metering duplication: kept CAP-9 as a Capability — task-spec2 Section 3 names token-level metering as required Problem Scope and Section 5 lists a token-metering engine as the CAP-1 exemplar, so demoting it to an Assumption would contradict the assignment's explicit scope. Removed the redundant SAFE Assumption restating the same fact; the Capability alone is unambiguous.
- (event) self-validate pass 1 (coherence): PASS. 13 capabilities each intent+success, CAP-1..13 unique/stable (12 unchanged, CAP-13 appended, no renumbering); 11 constraints all genuinely bend design (Kubernetes constraint reworded, not decorative); 8 non-goals unchanged; assumptions reduced 7->5 (Kubernetes-fact and token-metering duplicates removed as redundant once their Constraint/Capability states them plainly); 3 open questions unchanged; prose lean.
- (event) self-validate pass 2 (preservation): PASS. Future-window disclosure claim (previously dropped in the CAP-4 split) now preserved as CAP-13. No other source claims found missing on re-walk.
- (event) spec re-derived (targeted update, not full re-run): SPEC.md updated in place. 13 capabilities, 11 constraints (1 reworded), 8 non-goals, 5 assumptions, 3 open questions. Companions stakeholders.md and tone-of-voice.md unaffected.
- (direction by user) Targeted update pass, not a fresh draft: (1) sync spine to SPEC.md's new CAP-13 (future-availability-window disclosure, added in the peer-review remediation pass), (2) bring every AD up to task-spec2's required ADR format (Context & Problem, Decision Taken, Status, Consequences & Trade-offs, Rejected Alternatives), keeping existing Binds/Prevents/Rule fields intact.
- (question) CAP-13 needs a home: fold into AD-6's Outcome/details shape only, or does the sourcing behavior (compute from snapshot vs. live lookahead) deserve its own AD? User invited pushback rather than picking for me.
- (decision) Split it: AD-6 extended with the known_availability_window field (a pure shape addition, explicitly permitted by AD-6's own extend-via-new-field rule) plus a new AD-14 for the sourcing behavior. Reason: whether the window is computed from the same read-locked snapshot evaluate() already has, vs. a separate live lookahead query, is a real divergence risk two independently-built units could resolve incompatibly (violates AD-9 purity and AD-4's transaction boundary if done wrong) -- that's exactly the class of decision this spine exists to fix, not a shape detail.
- (decision) AD-14: known_availability_window is derived purely from the ReservationPort data already present in evaluate()'s snapshot parameter -- no additional query, null if no future reservation exists in that snapshot. Binds CAP-13. Rejected: (a) a live post-evaluate lookahead query by the driving adapter -- reopens the split-brain class AD-4 closes; (b) caching a background-computed prediction -- AD-2 already forbids anything but synchronous single-pass terminal decisions for this outcome.
- (decision) Upgraded AD-1 through AD-13 to full ADR format: added explicit Context & Problem, Decision Taken, Status (all Accepted -- none superseded, this pass only extends), Consequences & Trade-offs, and Rejected Alternatives to each, without discarding the existing Binds/Prevents/Rule fields (kept for implementer precision). Rejected Alternatives drawn from real considered options already implicit in this memlog's history (e.g. AD-4's SERIALIZABLE+retry, AD-1's held/pending status, AD-9's datetime.now() shortcut) rather than invented strawmen.
- (event) self-validate (coherence): PASS. 14 ADs, all with Binds/Context&Problem/Decision Taken/Status/Consequences&Trade-offs/Rejected Alternatives/Rule; no AD renumbered or reused; binds frontmatter and Capability-to-Architecture Map both updated for CAP-13 -> AD-6, AD-14; Deferred list needed no new entry (CAP-13 fully resolved by AD-6+AD-14, not deferred).
- (event) self-validate (reconciliation vs SPEC.md): PASS. All 13 SPEC capabilities (CAP-1..CAP-13) now bound to at least one AD; SPEC's Constraints rewording (Kubernetes/Kubeflow, from the same remediation pass) required no spine change -- Stack table and AD-12 deployment envelope already treat K8s/Kubeflow as the real integration target, consistent with the reworded constraint's plain statement of consequence.
- (event) spine updated (targeted pass, not full re-run): ARCHITECTURE-SPINE.md now carries 14 ADs (AD-14 new), full ADR-format fields on all of them, CAP-13 bound. status: final retained, updated timestamp unchanged (same day).
- (event) merge: this file consolidates the formerly-separate spec memlog (_bmad-output/specs/spec-gpu-governance/.memlog.md) and architecture memlog (_bmad-output/planning-artifacts/architecture/architecture-ia-development-project-2026-08-31/.memlog.md, now removed) into one append-only log, in true chronological order (spec creation -> architecture creation, which depended on the finalized spec -> spec peer-review remediation -> architecture sync to that remediation), per task-spec2's required one-.memlog.md-per-spec-folder layout. No entry content was altered; two identical constraint/non-goal/assumption/question blocks in the original spec log (lines 19-48 duplicating lines 49-78 of that file) are preserved as-is rather than deduplicated, since this log does not edit or reorder history.
- (event) file relocation: ARCHITECTURE-SPINE.md moved from planning-artifacts/architecture/ into this folder (sources: paths rewritten from ../../../specs/spec-gpu-governance/* to ./*); SPEC.md's companions frontmatter updated to list ARCHITECTURE-SPINE.md. Repo now matches task-spec2's required layout: SPEC.md + ARCHITECTURE-SPINE.md + .memlog.md + (companion-files/ still pending) under one spec-gpu-governance/ folder.
- (decision) Spawned companion-files/: glossary.md (spec-authored, canonical domain terms from SPEC.md+ARCHITECTURE-SPINE.md vocabulary; explicitly notes Preemption and Queuing/Waitlisting as deliberately absent, tying to the RISKY assumptions/Non-Goals rather than silently omitting them) and persona-archetypes.md (deep dive on the 5 stakeholders.md groups: motivation, friction, permission boundary tied to AD-5 privilege_level + specific CAP-x, and an explicit tension per persona -- Lab Operator's tension section names the CAP-8/stakeholders.md workload-distribution scope mismatch ARCHITECTURE-SPINE.md's Deferred section already flagged, rather than re-inventing or hiding it).
- (decision) Spawned companion-files/diagrams/: system-context.mmd (C4-style context graph -- actors, driving/core/driven hexagonal split, external K8s+Kubeflow+directory -- names match ARCHITECTURE-SPINE.md's Design Paradigm/Structural Seed exactly) and state-transitions.mmd (3 composite state machines: Reservation lifecycle, Accepted-job execution lifecycle, Machine status per CAP-8). Deliberately did not reproduce task-spec2's own example shape (Requested->Scheduled->Active->Preempted->Completed/Failed) verbatim: this SPEC has no queuing/Scheduled-holding state (AD-2) and no preemption (RISKY assumption) -- diagram states what the finalized spec actually does, with inline notes explaining both deliberate omissions, rather than contradicting the spec to match a generic template.
- (event) companion-files/ complete: glossary.md, persona-archetypes.md, diagrams/system-context.mmd, diagrams/state-transitions.mmd. SPEC.md companions frontmatter updated to include the two new .md companions.
- (event) Renamed design-artifacts/A-Product-Brief/01-product-brief.md -> project-brief.md to match task-spec2's required path exactly. Updated live cross-references: SPEC.md sources frontmatter, A-Product-Brief/00-product-brief.md phase-hub Documents table, design-artifacts/_progress/00-design-log.md. Left dialog/*.md (12-synthesis.md, decisions.md, progress-tracker.md) referencing the old filename as-is -- those are the Saga interview's own conversation trail, a historical record of that session, not a live index; renaming there would be editing history the same way hand-editing a .memlog.md would be. 00-product-brief.md itself (the Phase-1 hub template, distinct from the actual brief content) also left in place -- it's WDS process scaffolding, not a task-spec2 deliverable.
- (event) Full rubric self-audit against task-spec2 Section 7 (6 dimensions). Findings and fixes: (1) peer-review-remediation.md had gone stale after the second SPEC.md/ARCHITECTURE-SPINE.md edit pass -- line-number citations (CAP-1..12 vs actual 13, 7 vs actual 5 Assumptions, Non-Goals/Capability-Map line refs) were pointing at pre-edit positions; corrected all of them against the live files rather than leaving traceability claims unverifiable. (2) SPEC.md Assumptions used (SAFE)/(RISKY) parenthetical tags; task-spec2's own Spec Law wording specifies [Safe]/[Risky] bracket tags -- reformatted to match literally, no content change. (3) Verified: zero implementation-leakage vocabulary (postgres/kubernetes/k8s/redis/fastapi/websocket/etc.) anywhere in the Capabilities section across all 13 CAPs; ARCHITECTURE-SPINE.md's 14 AD headers sequential with no gaps/dupes, code-fence and mermaid-block counts balanced; both companion .mmd diagrams have balanced subgraph/end and composite-state/note pairs. No further defects found.
